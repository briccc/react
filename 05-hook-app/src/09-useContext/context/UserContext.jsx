import { createContext } from 'react';

export const UserContext = createContext();  //react component, puede tener hijos

//es util para que todos los componentes tengan acceso a la informacion del usuario
